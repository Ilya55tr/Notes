spring:
  datasource:
    url: jdbc:postgresql://localhost:${DATA_HOST}/${DATA_NAME}
    username: ${DATA_USERNAME}
    password: ${DATA_PASSWORD}
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml